import api from '../index'
import urls from './url'
// 这个东西是unsplash要求加在header里的验证

export default {
  baseInterface (params) {
    // return出去了一个promise
    return api.post(urls.commonUrl, params )
  }
}
